name = "Nanaloveyuki/orbit-plugin-abi"

version = "0.2.0"

description = "Fixed, auditable native ABI bridge for Orbit plugins."

repository = "https://github.com/Nanaloveyuki/orbit-plugin-abi"

license = "Apache-2.0"

readme = "README.md"

keywords = [ "moonbit", "orbit", "plugin", "native", "ffi" ]

preferred_target = "native"

source = "src"

import {
  "Nanaloveyuki/dynlib@0.1.0",
}
